import '../../src/memory.css'

export const metadata = {
  title: 'Memory · UGBZ',
  description: 'Der Familienklassiker mit euren eigenen Motiven.',
}

export const viewport = {
  themeColor: '#0f3b2c',
  width: 'device-width',
  initialScale: 1,
}

export default function MemoryLayout({ children }) {
  return children
}
